{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "build:declarations": {
      "dependsOn": ["^build:declarations"],
      "outputs": ["types/**"]
    },
    "lint": {},
    "lint:fix": {},
    "typecheck": {}
  }
}
