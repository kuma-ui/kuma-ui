import { NextPage } from "next";
import { redirect } from "next/navigation";

type PageProps = {
  params: {
    slug: string[];
  };
};

const Home: NextPage = (props) => <></>;

export default Home;
