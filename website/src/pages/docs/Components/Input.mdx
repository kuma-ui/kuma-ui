---
title: "Input"
description: "The Input component inherits from the HTML input element and can be combined with Kuma UI's style properties for customization."
---

import {

ExampleContainer,
} from "../../../components/example";

# Input

The Input component inherits from the HTML input element and can be combined with Kuma UI's style properties for extensive customization.

## Import

```tsx copy
import { Input } from "@kuma-ui/core";
```
