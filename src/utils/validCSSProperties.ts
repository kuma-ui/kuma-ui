import { combinedStyles } from "../system";

// const validCSSProperties: { [key: string]: boolean } = (() => {
//   const styleKeys = Object.keys(combinedStyles.mappings);
//   const properties: { [key: string]: boolean } = {};

//   styleKeys.forEach((key) => {
//     const cssProperty = combinedStyles.mappings[key];
//     if (cssProperty) {
//       properties[cssProperty] = true;
//     }
//   });

//   return properties;
// })();
